# Changelog

## 0.1.1 — 2026-06-27

- Add CLI with argparse (`--watch`, `--interval`, `--alert-process`, `--output`)
- Add `config.py` with JSON configuration loader/saver
- Improve README with CLI usage and project structure
- Update pyproject.toml with metadata, classifiers, and entry point
